import { activeClass } from 'chathub-ember/helpers/active-class';
import { module, test } from 'qunit';

module('Unit | Helper | active class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = activeClass([42]);
  assert.ok(result);
});
